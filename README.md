# DePing CLI Daemon

A high-performance DePIN Uptime Monitor for the DePing network.

## 🚀 Quick Install

To install the latest version of the DePing CLI, run the following command in your terminal:

```bash
curl -L [https://raw.githubusercontent.com/everestp/deping-cli-daemon/main/install.sh](https://raw.githubusercontent.com/everestp/deping-cli-daemon/main/install.sh) | bash